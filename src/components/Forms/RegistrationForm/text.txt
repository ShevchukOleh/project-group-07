// =================================Current user data
// const isTiken = useSelector(selectToken);
// console.log(isTiken);
// useEffect(() => {
  // dispatch(fetchCurrentUser(isTiken));
// }, [isTiken, dispatch]);
// ====================================

LogOut==============================>
 const isTiken = useSelector(selectToken);
 console.log(isTiken);
 useEffect(() => {
   dispatch(logoutUser(isTiken));
 }, [isTiken, dispatch]);